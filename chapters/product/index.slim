---
layout: chapter
title: Product
chapter: Product
---

{% capture url %}/chapters/{{ page.chapter | replace: ' ', '_' | downcase }}/{% endcapture %}

ul
{% for page in site.pages %}
  {% if page.url contains url %}
    {% unless page.url == url %}
  li
    a href="{{ page.url | replace: '.html', '' }}" {{ page.title }}
    {% endunless %}
  {% endif %}
{% endfor %}
